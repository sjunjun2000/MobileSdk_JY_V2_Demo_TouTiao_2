<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!-- JyLoginDialog -->
    <style name="EditTextStyle" parent="AppTheme">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="ButtonStyle">
        <item name="android:textSize">@dimen/jy_size_register_large</item>
        <item name="android:gravity">center</item>
        <item name="background">@drawable/jy_selector_register_btn</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/jy_spacing_tab_register</item>
        <item name="android:visibility">gone</item>
        <item name="android:padding">@dimen/jy_spacing_tab_register</item>
        <item name="android:textColor">@color/jy_selector_register_btn</item>
    </style>

    <!-- my loading dialog -->
    <style name="LoadingDialogStyle">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- popup window 显示消失动画-->
    <style name="LoadingPopWindowAnimStyle">
        <item name="android:windowShowAnimation">@anim/jy_dialog_enter</item>
        <!-- 指定显示的动画xml  -->
        <item name="android:windowHideAnimation">@anim/jy_dialog_exit</item>
        <!-- 指定消失的动画xml  -->
    </style>


</resources>
